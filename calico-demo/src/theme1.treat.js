import { createTheme } from 'treat';
import { theme as calicoTheme } from './themeTokens';

// export default createTheme({
//     brandColor: 'gold',
//     grid: 4,
// });

export const theme = createTheme(calicoTheme);
